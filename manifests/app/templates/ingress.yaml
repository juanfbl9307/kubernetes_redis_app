apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}-ingress
  namespace: {{ .Chart.Name }}
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              service:
                name: {{ .Chart.Name }}-service
                port:
                  number: 80